{% extends "layouts/header.njk" %}
{% block main %}
  <section id="main">
    <div class="container">
      <header>
        <h2>My projects ({{ projectCount }})</h2>
        <div class="addon">
          <form action="/dashboard/newproject" method="POST">
            <input type="text" name="name" placeholder="New project..." required>
            <button type="submit" class="btn">
              <img src="/img/plus.svg">
            </button>
          </form>
        </div>
      </header>
      <section id="content">
        <div class="project-list">
          {% if projects != null %}
            {% for project in projects %}
              <a href="/dashboard/{{ userId }}/project/{{ project.id }}">{{ project.name }}</a>
            {% endfor %}
          {% else %}
            <h3>You don't have any project. Start by creating a new project clicking in "+"</h3>
          {% endif %}
        </div>
      </section>
    </div>
  </section>
{% endblock %}
